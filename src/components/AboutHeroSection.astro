---
import { Picture } from 'astro:assets';
import SectionLabel from './SectionLabel.astro';
import aboutHeroImage from '../assets/images/about-hero-video.png';
---

<section class="w-full">

	<div class="container-site pt-8">
		<div class="video-overlay-wrapper focus-ring relative rounded-2xl overflow-hidden w-full aspect-video max-h-80 cursor-pointer hover:cursor-none" role="button" tabindex="0" aria-label="Reproducir video">
			<div class="absolute inset-0" aria-hidden="true">
				<Picture
					src={aboutHeroImage}
					formats={['avif', 'webp']}
					alt=""
					class="w-full h-full object-cover opacity-80"
					width={1024}
					height={576}
				/>
				<div class="absolute inset-0 bg-black/40" />
			</div>
			<span
				class="video-overlay-label absolute left-1/2 top-1/2 flex -translate-x-1/2 -translate-y-1/2 items-center gap-2 pointer-events-none z-10 text-sm font-semibold text-white whitespace-nowrap drop-shadow-[0_1px_2px_rgba(0,0,0,0.5)]"
				aria-hidden="true"
			>
				<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" class="size-5 shrink-0 fill-current" aria-hidden="true">
					<path d="M8 5v14l11-7z"/>
				</svg>
				Reproducir video
			</span>
		</div>
	</div>

	<div class="container-site flex flex-col lg:flex-row gap-12 lg:gap-20 py-24">

		<div class="flex-1 flex flex-col gap-2">
			<SectionLabel label="Quiénes somos" />
			<h1 class="font-display font-bold text-4xl leading-tight text-navy-blue">
				En Dental Gran Avenida<br />tenemos +40 años de historia
			</h1>
		</div>

		<div class="flex-1 flex items-center">
			<div class="flex flex-col gap-4 text-base text-body">
				<p>
					En Dental Gran Avenida entendemos que una sonrisa sana y armónica impacta directamente en tu bienestar y confianza. Somos una clínica odontológica integral que combina excelencia clínica, tecnología de vanguardia y un trato humano y personalizado.
				</p>
				<p>
					Nuestro equipo de profesionales altamente capacitados ofrece tratamientos dentales avanzados, estética dental de alto estándar y atención de urgencia, acompañándote con precisión, calidez y resultados duraderos.
				</p>
			</div>
		</div>

	</div>
</section>

<script>
	document.querySelectorAll('.video-overlay-wrapper').forEach((wrapper) => {
		const label = wrapper.querySelector('.video-overlay-label');
		if (!label) return;

		wrapper.addEventListener('mouseenter', () => {
			wrapper.classList.add('is-hovering');
			label.classList.add('fixed', '-translate-x-1/2', '-translate-y-1/2');
			label.classList.remove('absolute', 'left-1/2', 'top-1/2');
		});

		wrapper.addEventListener('mouseleave', () => {
			wrapper.classList.remove('is-hovering');
			label.classList.remove('fixed');
			label.classList.add('absolute', 'left-1/2', 'top-1/2', '-translate-x-1/2', '-translate-y-1/2');
			label.style.left = '';
			label.style.top = '';
		});

		wrapper.addEventListener('mousemove', (e: MouseEvent) => {
			if (wrapper.classList.contains('is-hovering')) {
				label.style.left = `${e.clientX}px`;
				label.style.top = `${e.clientY}px`;
			}
		});
	});
</script>
