---
import Card from './Card.astro';
import SectionHeading from './SectionHeading.astro';

interface Props {
	background?: 'white' | 'blue-white';
}

const { background = 'white' } = Astro.props;
const bgClass = background === 'blue-white' ? 'bg-blue-white' : 'bg-white';

const values = [
	{
		title: 'Misión',
		description:
			'Brindar atención odontológica integral de alta calidad, centrada en el paciente y su familia, con tecnología de vanguardia, profesionales altamente capacitados y un trato humano y cercano.',
	},
	{
		title: 'Visión',
		description:
			'Ser la clínica dental de referencia en El Bosque y Gran Avenida, reconocida por la excelencia clínica, la innovación continua y el compromiso con el bienestar y la sonrisa de nuestros pacientes.',
	},
];
---

<section class:list={[bgClass, 'py-24 w-full']}>
	<div class="container-site flex flex-col lg:flex-row items-center lg:items-start gap-20">

		<!-- Left: Misión + Visión cards — order-2 on mobile so heading appears first -->
		<div class="flex flex-col sm:flex-row lg:flex-col gap-8 flex-1 order-2 lg:order-1 items-center lg:items-stretch">
			{values.map((v) => (
				<Card variant="blue" class="flex-1 w-full sm:w-auto">
					<div class="flex flex-col gap-2 text-center lg:text-left">
						<h3 class="font-display font-bold text-xl text-navy-blue">{v.title}</h3>
						<p class="text-base text-body">{v.description}</p>
					</div>
				</Card>
			))}
		</div>

		<!-- Right: Section heading — first on mobile, sticky on desktop -->
		<div class="flex-1 flex items-center lg:items-start lg:sticky lg:top-24 lg:self-start lg:pt-0 order-1 lg:order-2 w-full">
			<SectionHeading
				label="Misión y más"
				title="Conoce nuestros valores y propósito"
				description="Llevamos más de 40 años comprometidos con la salud bucal de las familias de El Bosque. Nuestros valores guían cada decisión clínica y cada interacción con nuestros pacientes."
				centerOnMobile
			/>
		</div>

	</div>
</section>

